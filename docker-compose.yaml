version: "3"
services:
  build-image:
    image: fpco/stack-build:lts-13.21
    working_dir: /work
    volumes:
    - .:/work
    - ~/.stack:/root/.stack
    - ./.dump:/root/.local/bin
    command: ./install.bash
